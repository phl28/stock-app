{
	"functions": {
		"api/*.py": {
			"memory": 1024,
			"maxDuration": 10
		}
	},
	"rewrites": [{ "source": "/api/(.*)", "destination": "/api/main.py" }],
	"buildCommand": "npm run db:migrate && npm run build"
}
